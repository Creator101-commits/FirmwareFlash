{
  "id": "mqtt-publisher",
  "name": "MQTT Publisher",
  "description": "Connect to a WiFi network and publish a message to an MQTT broker. ESP32 only.",
  "compatibleBoards": ["ESP32", "CH340 (common ESP32 clone)"],
  "requiredLibraries": ["WiFi", "PubSubClient"],
  "setupCode": "  Serial.begin(115200);\n  WiFi.begin(\"SSID\", \"PASSWORD\");\n  while (WiFi.status() != WL_CONNECTED) { delay(500); }\n  mqttClient.setServer(\"broker.hivemq.com\", 1883);\n  if (mqttClient.connect(\"FirmwareFlashClient\")) {\n    mqttClient.publish(\"firmware/flash\", \"hello from device\");\n  }",
  "loopCode": "  if (!mqttClient.connected()) {\n    if (mqttClient.connect(\"FirmwareFlashClient\")) {\n      mqttClient.publish(\"firmware/flash\", \"ping\");\n    }\n  }\n  mqttClient.loop();\n  delay(5000);"
}
